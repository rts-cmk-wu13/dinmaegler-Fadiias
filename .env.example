# Frontend
VITE_APP_TITLE=Din Mægler
VITE_AUTH_API=http://localhost:4000

# Backend
# If your host sets PORT, you can omit AUTH_PORT
# AUTH_PORT=4000

# CORS_ORIGIN can be a single origin or comma-separated list, or *
# CORS_ORIGIN=http://localhost:5173

# STORAGE_DIR is where usersStore.json will be kept on the server
# STORAGE_DIR=./server